User events are required to simulate a click or entering a value into an input.
the following user actions are available:

import user from '@testing-library/user-event';

user.click(element) -> simulate click at element
user.keyboard("text") -> Simulate typing "test"
user.keyboard("{Enter}") -> Simulate pressing Enter key